overwrite: true
schema: "https://api.spacex.land/graphql"
documents: null
generates:
  output:
    preset: dist/my-preset.js
    presetConfig:
      variables:
        - ship_query:
            - id: HAWK
    plugins:
      - dist/empty.js
