overwrite: true
schema: "https://api.spacex.land/graphql"
generates:
  output:
    preset: dist/preset.js
    presetConfig:
      variables:
        - ship_query:
            - id: HAWK
    plugins:
      - dist/empty.js
