overwrite: true
schema: "https://api.spacex.land/graphql"
generates:
  output:
    preset: dist/preset.js
    presetConfig:
      host: https://api.spacex.land/graphql
      variables:
        ship_query:
          id: HAWK
      include: cores_query
    plugins:
      - dist/empty.js
