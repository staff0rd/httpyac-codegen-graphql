overwrite: true
schema: "https://api.spacex.land/graphql"
generates:
  output:
    preset: dist/httpyac-codegen-graphql.js
    presetConfig:
      variables:
        - ship_query:
            - id: HAWK
    plugins:
      - dist/empty.js
